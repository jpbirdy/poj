#include <iostream>
#include <cmath>
#include <cstdlib>
#include <cstdio>
#include<time.h>
using namespace std;

const int N = 100000000;
const int M = 1;

int main()
{
    int a;
    clock_t start_time,end_time;

    start_time=clock();
    for(int i=0 ; i<M ; i++)
        for(int j=0 ; j<N ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);

    start_time=clock();
    for(int i=0 ; i<M ; i++)
        for(int j=0 ; j<N ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
        }
    end_time=clock();
    printf("%d\n",end_time-start_time);
    start_time=clock();
    for(int i=0 ; i<M ; i++)
        for(int j=0 ; j<N ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);
    start_time=clock();
    for(int i=0 ; i<M ; i++)
        for(int j=0 ; j<N ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);


    start_time=clock();
    for(int i=0 ; i<M ; i++)
        for(int j=0 ; j<N ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);

start_time=clock();
    for(int i=0 ; i<M ; i++)
        for(int j=0 ; j<N ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<M ; i++)
        for(int j=0 ; j<N ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<M ; i++)
        for(int j=0 ; j<N ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<M ; i++)
        for(int j=0 ; j<N ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<M ; i++)
        for(int j=0 ; j<N ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<M ; i++)
        for(int j=0 ; j<N ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<M ; i++)
        for(int j=0 ; j<N ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<M ; i++)
        for(int j=0 ; j<N ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<M ; i++)
        for(int j=0 ; j<N ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<M ; i++)
        for(int j=0 ; j<N ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<M ; i++)
        for(int j=0 ; j<N ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<M ; i++)
        for(int j=0 ; j<N ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<M ; i++)
        for(int j=0 ; j<N ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<M ; i++)
        for(int j=0 ; j<N ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<M ; i++)
        for(int j=0 ; j<N ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);



    printf("b\n");
    start_time=clock();
    for(int i=0 ; i<N ; i++)
        for(int j=0 ; j<M ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);


    start_time=clock();
    for(int i=0 ; i<N ; i++)
        for(int j=0 ; j<M ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);
    start_time=clock();
    for(int i=0 ; i<N ; i++)
        for(int j=0 ; j<M ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);

    start_time=clock();
    for(int i=0 ; i<N ; i++)
        for(int j=0 ; j<M ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);


    start_time=clock();
    for(int i=0 ; i<N ; i++)
        for(int j=0 ; j<M ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
        }
    end_time=clock();
    printf("%d\n",end_time-start_time);

    start_time=clock();
    for(int i=0 ; i<N ; i++)
        for(int j=0 ; j<M ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<N ; i++)
        for(int j=0 ; j<M ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<N ; i++)
        for(int j=0 ; j<M ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<N ; i++)
        for(int j=0 ; j<M ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<N ; i++)
        for(int j=0 ; j<M ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<N ; i++)
        for(int j=0 ; j<M ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<N ; i++)
        for(int j=0 ; j<M ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<N ; i++)
        for(int j=0 ; j<M ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<N ; i++)
        for(int j=0 ; j<M ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<N ; i++)
        for(int j=0 ; j<M ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<N ; i++)
        for(int j=0 ; j<M ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<N ; i++)
        for(int j=0 ; j<M ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<N ; i++)
        for(int j=0 ; j<M ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<N ; i++)
        for(int j=0 ; j<M ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);start_time=clock();
    for(int i=0 ; i<N ; i++)
        for(int j=0 ; j<M ; j++)
        {
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);
            a = sin(rand()*360);

        }
    end_time=clock();
    printf("%d\n",end_time-start_time);

    return 0;
}
